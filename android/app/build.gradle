plugins {
    id "com.android.application"
    id "org.jetbrains.kotlin.android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.raymond_test"
    compileSdk = 34 // Explicit compile SDK version
    ndkVersion = "22.1.7171670" // Ensure you have the correct NDK version

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
        applicationId = "com.example.raymond_test"
        minSdk = 21 // Specify the minimum SDK version
        targetSdk = 33 // Explicit target SDK version
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            // Signing config (update if you have a release signing config)
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:1.9.10" // Latest Kotlin standard library
}
